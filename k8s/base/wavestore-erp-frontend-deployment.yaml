apiVersion: apps/v1
kind: Deployment
metadata:
  name: wavestore-erp-frontend
  labels:
    app: wavestore-erp-frontend
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: wavestore-erp-frontend
  template:
    metadata:
      labels:
        app: wavestore-erp-frontend
        component: frontend
    spec:
      containers:
      - name: wavestore-erp-frontend
        image: ghcr.io/willeastbury/wavestore-erp-frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8805
          name: http
        env:
        - name: PORT
          value: "8805"
        - name: WAVE_STS_URL
          value: ""
        - name: WAVESTORE_ERP_API_URL
          value: ""
        - name: LOG_LEVEL
          value: "info"
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8805
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8805
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: wavestore-erp-frontend
  labels:
    app: wavestore-erp-frontend
spec:
  type: ClusterIP
  ports:
  - port: 8805
    targetPort: 8805
    protocol: TCP
    name: http
  selector:
    app: wavestore-erp-frontend
